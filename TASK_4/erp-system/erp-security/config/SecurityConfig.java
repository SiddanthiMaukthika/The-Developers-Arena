package com.erp.security.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Simplified version (no full Spring Security config)
}