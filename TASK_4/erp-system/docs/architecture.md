# Architecture

The ERP system follows a Modular Monolith Architecture.

Each module is independently structured with:
Controller → Service → Repository → Entity

Benefits:

* Scalability
* Maintainability
* Clear separation of concerns
